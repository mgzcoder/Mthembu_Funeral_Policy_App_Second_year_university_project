@model IEnumerable<Mthembu_Funeral_Policy_App.Models.tlbBeneficiary>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Beneficiary_SA_ID_NUmber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tlbClient_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Beneficiary_First_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Beneficiary_last_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Beneficiary_Relationship)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Beneficiary_SA_ID_NUmber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tlbClient_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beneficiary_First_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beneficiary_last_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beneficiary_Relationship)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit","", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
