@model IEnumerable<Mthembu_Funeral_Policy_App.Models.tlbPolicy>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tlbClient_ID2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Policy_Cost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Available_Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Policy_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cover_Type)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tlbClient_ID2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Policy_Cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Available_Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Policy_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cover_Type)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Policy_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.Policy_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Policy_ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
